import { MasterLayout } from '@/components/layout';

export default function WorkspacePage() {
  // CenterPanel renders ChatPanel by default when no centerContent is provided
  return <MasterLayout />;
}

